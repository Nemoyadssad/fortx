# URL of the NestJS API. Defaults to http://localhost:4000 if unset.
NEXT_PUBLIC_API_URL=https://api.fortx.world

# Show the "+1000 test credits" button in the header. Set to "false" to hide it.
NEXT_PUBLIC_DEV_TOPUP=true